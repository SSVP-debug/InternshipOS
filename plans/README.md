# Implementation plans

Generated by an `/improve`-style audit on 2026-09-12, stamped against
commit `e311775` on `master`.

## Status

| Plan | Finding | Leverage | Status | Depends on |
|---|---|---|---|---|
| [001](001-fix-zod-v4-uuid-test-fixtures.md) | Zod v3→v4 bump broke UUID validation in 6 test files (55 of 59 currently-failing tests) | High — small fix, currently-red CI | **DONE** | none |
| [002](002-fix-stale-adapter-test-fixture-counts.md) | Stale hardcoded fixture counts in 2 adapter test files (remaining 4 of 59 failing tests) | Medium — small fix, restores real regression coverage | **DONE** | none |

Plans 001 and 002 are independent — different root causes, disjoint
files. Do them in either order, or in parallel.

Together they should take the backend suite from **59 failed / 682
passed (741 total)** back to **0 failed / 741 passed**, with `tsc
--noEmit` staying clean throughout (it already is).

## Considered and rejected / deferred (not planned)

- **Transitive `qs` dependency, moderate DoS/parsing advisories**
  (GHSA-x5fp-wj9c-mxmx, GHSA-4mjr-xmp4-gh2g). Low impact (transitive,
  no evidence of a directly attacker-reachable path), one-line fix
  (`npm audit fix`). Not worth a formal plan file — just run it.
- **"Auto-apply" scope gap** (product intent was automatic external
  application submission; current `bulk-apply` only creates an
  internal tracker row). This is a deliberate, previously-recorded
  scope decision (`docs/gate-r0-resume-design.md`: "Real
  form-submission/browser automation — explicitly out of scope per
  your Gate R0 answer"), not a bug. It's a product direction question
  for the maintainer, not something to plan without an explicit
  decision to revisit that scope call.
- **`PROGRESS.md` / `README.md` stale test-count claims** — worth a
  follow-up doc correction once Plans 001–002 land, but that's a
  one-line doc edit once the numbers are known, not worth a formal plan.

## After these land

Once both plans are executed and the suite is fully green again, it's
worth adding a note to `PROGRESS.md` (or wherever the next handover
entry goes) recording that the zod v4 bump required a fixture fix — so
a future dependency bump on a validation library gets the same
"actually run the tests, don't just check `tsc --noEmit`" scrutiny this
one didn't get before merging.
